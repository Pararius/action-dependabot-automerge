---
name: ci
on:
  pull_request:
  push:
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Lint source
        run: make lint
      - name: Compile source
        run: make compile
      - name: Build source
        run: make build
      - name: Run tests
        run: make test
      - name: Check for compile/build changes
        run: |
          if [[ -n $(git status --porcelain) ]]; then
            echo "git working directory dirty, probably forgotten to include compiled source"
          fi
